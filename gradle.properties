# Java toolchain — set to 25 in CI environments where Java 25 is installed
# Override via: ./gradlew -PjavaVersion=25 build
javaVersion=21

org.gradle.java.installations.auto-provision=false
org.gradle.caching=true
